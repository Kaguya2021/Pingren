services:
  - type: worker
    name: render-ping-bot
    env: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DATABASE_PATH
        value: data/bot.db
    disk:
      name: sqlite-data
      mountPath: /app/data
      sizeGB: 1
